package ${app.packageName}.config;

import net.ymate.platform.configuration.annotation.Configuration;
import net.ymate.platform.configuration.impl.DefaultConfiguration;

/**
 * ${app.name?cap_first}RepositoryConfig
 * <br>
 * Code generation by yMateScaffold on ${.now?string("yyyy/MM/dd a HH:mm")}
 *
 * @author ${app.author?default("ymatescaffold")}
 * @version ${app.version?default("1.0.0")}
 */
@Configuration("${cfgFileName}")
public class ${app.name?cap_first}RepositoryConfig extends DefaultConfiguration {
}